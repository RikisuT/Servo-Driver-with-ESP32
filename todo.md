Proposed code improvements (human editing only)

- GUI should disable controlls or show errors on impossible commands like "Middle" while in motor mode
- Finer grain settings on Speed, maybe manually enter a number
- Remove Rainbow
- Direct position input (slider or text field) instead of only increment/decrement
- Servo limits configuration (min/max angle, max torque, max temp)
- Preset positions / simple motion sequences for testing
- Visual warnings for over-temperature / over-load
- Connect to home network - captive portal w/ WiFiManager library or custom. No initial password