// WiFi STA credentials — copy this file to wifi_credentials.h and fill in your values.
// wifi_credentials.h is gitignored and will not be committed.
const char* STA_SSID = "YOUR_SSID";
const char* STA_PWD  = "YOUR_PASSWORD";
